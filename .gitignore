# Python
.venv/
__pycache__/
*.pyc
*.egg-info/

# py2app build artifacts (macapp/build_app.sh)
build/
dist/
macapp/.cache/

# macOS
.DS_Store

# Personal Claude session notes
CLAUDE.md
.claude/

# Local state (never commit user config, dictation history, dictionaries, or logs)
*.log
*.bak
.env
.env.*
history*.jsonl
dictionary.txt
config.toml

# Editor
.idea/
.vscode/

# wrangler local dev state — contains machine geolocation (cf.json), the
# Cloudflare account id, and local D1 data; must never be committed
telemetry-server/.wrangler/
