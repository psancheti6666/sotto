parakeet-mlx ; sys_platform == "darwin" and platform_machine == "arm64"
onnx-asr[cpu,hub] ; sys_platform != "darwin" or platform_machine != "arm64"
pynput ; sys_platform == "darwin" or sys_platform == "win32"
pyobjc-framework-AVFoundation ; sys_platform == "darwin"
pyobjc-framework-WebKit ; sys_platform == "darwin"
pyobjc-framework-UserNotifications ; sys_platform == "darwin"
evdev ; sys_platform == "linux"
zstandard ; sys_platform == "linux"
# tray icon (best-effort; docs/linux-app.md L7). PyGObject — needed for the
# appindicator backend GNOME requires — is NOT here: it compiles against
# system gir headers, which would break `pip install -r` on a bare checkout.
# build_app.sh installs it (best-effort) for the bundle; checkouts get
# pystray's gtk/xorg fallback or the tray-less log line. Minor-pinned: it
# ships inside signed release artifacts with keyboard access — a hijacked
# upstream release must not ride in silently (L7 security sweep).
pystray>=0.19,<0.20 ; sys_platform == "linux" or sys_platform == "win32"
# Insights window on WebView2 (docs/windows-app.md W8). Minor-pinned like
# pystray: ships inside artifacts that render local user data.
pywebview>=5.3,<6 ; sys_platform == "win32"
sounddevice
requests
pyperclip
rapidfuzz
